# J.C.S. Ultimate Combat Bar

Current release: **v1.6 RC32 CS1**

Each published version is kept in its own folder so older releases remain available and each version stays self-contained.

- `v1.6-RC32/` — current release; includes `JCS_CombatBar_RC32_CS1.py` and the matching README.
- `v1.6-RC16/` — previous public release preserved for reference.
- `v1.6-RC15/` and `v1.6-RC14/` — older public releases preserved for rollback/reference.

Use the newest version folder unless you specifically need to test an older build.
